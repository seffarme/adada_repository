<!-- TITLE -->
<div class="card-trip mb-4">
    <h3 id="category-top-right"><%= @transport.category %></h3>
    <%= cl_image_tag @transport.photo.key, crop: :fill %>
    <div class="card-trip-infos">
      <div>
        <h2><%= @transport.name %></h2>
        <p><%= @transport.description %></p>
      </div>
      <h3 class="card-trip-pricing"><%= number_with_delimiter(@transport.price, :delimiter => ',') %>€</h3>
      <%= cl_image_tag @transport.owner.photo.key, crop: :fill, class:"card-trip-user avatar-bordered" %>
    </div>
  </div>

<%= link_to "Back to list", transports_path, class:"btn btn-primary" %>


<%
=begin%>
 <%
  if @transport.photo.attached?
    url = cl_image_path @transport.photo.key, height: 300, width: 500, crop: :fill
  else
    url = "https://images.ladepeche.fr/api/v1/images/view/5eca937b8fe56f2f05260255/large/image.jpg?v=1"
  end
%>
<%
=end%>
<%
=begin%>
 <%= cl_image_tag @transport.photo.key, height: 300, width: 400, crop: :fill %>
<%
=end%>

<h3><%= @transport.category %></h3>
<h2><%= @transport.name %></h2>
<p><%= @transport.description %></p>
<h2><%= @transport.price %></h2>

<%= link_to "Back to list", transports_path %> |

<%= link_to "Delete #{@transport.name}",
            transport_path(@transport),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<div class="text-center" >
  <% if @available %>
   <button class="btn btn-primary m-3">Book Now</button>
  <% else %>
   <button class="btn btn-primary m-3">Booked</button>
  <% end %>
</div>

