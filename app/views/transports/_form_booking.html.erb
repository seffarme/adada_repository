<div class="form-wrapper" id="booking-form-div" data-bookings="<%= @bookings_dates.to_json %>">
  <h2>Book your Transport</h2>
  <%= simple_form_for [@transport, @booking] do |f| %>
    <%= f.input :start_date, label: "Sélectionnez vos dates:", as: :string, required: false, input_html: { id: "range_start", class: "d-none"} %>
    <%= f.input :end_date, label: false, as: :string, required: false, input_html: { id: "range_end", class: "d-none" } %>
      <button type="button" class="btn btn-info btn-lg m-3" data-toggle="modal" data-target="#myModal" btn.disabled = true>   <%= f.button :submit, "Book Now" %>
      </button>
  <% end %>
</div>
